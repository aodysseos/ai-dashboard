# Client Configuration
VITE_API_URL=http://localhost:3001
VITE_APP_NAME=AI Dashboard

# Docker Notes:
# - For local development: Use http://localhost:3001
# - For Docker development: Use http://localhost:3001 (Docker forwards the port)
# - For production: Update VITE_API_URL to your actual API domain
# - If using Docker Compose with API proxy through Nginx, you can use relative URLs (/api)
