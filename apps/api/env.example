# API Configuration
API_PORT=3001
# For Docker: Use 0.0.0.0 to bind to all interfaces
# For local development: Use localhost
API_HOST=localhost
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# AWS S3 Configuration
# Note: These values are only used in production (NODE_ENV=production)
# Development automatically uses Minio (no AWS credentials needed)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
S3_BUCKET_NAME=your_bucket_name_here
S3_UPLOAD_EXPIRATION=300

# Storage Configuration:
# - Development (NODE_ENV=development): Uses Minio automatically
#   - Bucket: ai-dashboard-dev (auto-created)
#   - Credentials: minioadmin/minioadmin (hardcoded)
#   - Endpoint: http://minio:9000 (in Docker)
#   - Public Endpoint: http://localhost:9000 (for browser access)
# - Production (NODE_ENV=production): Uses AWS S3
#   - Requires AWS credentials above
#   - Uses bucket name from S3_BUCKET_NAME

# S3 Public Endpoint (for pre-signed URLs accessible from browser)
# Only needed in development when running in Docker
S3_PUBLIC_ENDPOINT=http://localhost:9000

# Docker Notes:
# - When running in Docker, API_HOST should be 0.0.0.0
# - The CLIENT_URL should match your client's accessible URL
# - In Docker Compose, services communicate using service names (e.g., http://api:3001)
