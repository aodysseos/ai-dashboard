# Docker Environment Configuration
# Copy this file to .env in the root directory for Docker Compose

# ======================
# API Configuration
# ======================
API_PORT=3001
API_HOST=0.0.0.0
NODE_ENV=production
CLIENT_URL=http://localhost

# ======================
# AWS S3 Configuration
# ======================
# Note: Only required for production (NODE_ENV=production)
# Development uses Minio automatically with no AWS credentials needed
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
S3_BUCKET_NAME=your_bucket_name_here
S3_UPLOAD_EXPIRATION=300

# S3 Public Endpoint (for pre-signed URLs accessible from browser)
# Development: Set to http://localhost:9000 for Minio
# Production: Leave unset to use default AWS S3 endpoints
S3_PUBLIC_ENDPOINT=http://localhost:9000

# ======================
# Client Configuration
# ======================
VITE_API_URL=http://localhost:3001
VITE_APP_NAME=AI Dashboard

# ======================
# Docker Notes
# ======================
# 1. For development:
#    - Use docker-compose.dev.yml
#    - Set NODE_ENV=development
#    - CLIENT_URL=http://localhost:5173
#    - Minio automatically used for S3-compatible storage
#    - No AWS credentials required
#
# 2. For production:
#    - Use docker-compose.yml
#    - Set NODE_ENV=production
#    - Update CLIENT_URL to your production domain
#    - Update VITE_API_URL to your production API domain
#    - AWS S3 credentials required
#
# 3. Port Mappings:
#    - API: localhost:3001 -> container:3001
#    - Client (dev): localhost:5173 -> container:5173
#    - Client (prod): localhost:80 -> container:80
#    - Minio (dev): localhost:9000 (API), localhost:9001 (Console)
#
# 4. Service Communication:
#    - Services communicate using Docker network service names
#    - API is accessible at http://api:3001 from within the Docker network
#    - Minio is accessible at http://minio:9000 from within the Docker network
#
# 5. Storage Configuration:
#    - Development: Minio (S3-compatible)
#      - Console: http://localhost:9001 (minioadmin/minioadmin)
#      - Bucket: ai-dashboard-dev (auto-created)
#    - Production: AWS S3
#      - Requires valid AWS credentials above
#
# 6. Security:
#    - Never commit .env files with real credentials
#    - Use secrets management for production deployments
#    - Consider using AWS IAM roles instead of access keys when possible

